# python-SMS-Automation
This Python script uses the Twilio API to send an SMS message to a specific phone number. It authenticates using the Account SID and Auth Token, then creates a client instance to send a custom text message from a Twilio number to a receiver’s number, enabling fast and automated messaging.
